<button type="button" class="btn btn-{{ $btnClass ?? 'text'}}" data-toggle="modal"
    data-target="#{{ $dataTarget ?? 'text'}}"><i class="{{ $icon ?? 'text'}}"></i>
    {{ $name ?? 'text' }}
</button>
